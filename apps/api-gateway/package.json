{
  "name": "@agent-optima/api-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "npm run typecheck",
    "test": "node --test",
    "dev": "node --env-file=../../.env --watch --experimental-specifier-resolution=node dist/index.js",
    "start": "node --env-file=../../.env dist/index.js"
  },
  "dependencies": {
    "@agent-optima/fastify-auth": "*",
    "@agent-optima/queue": "*",
    "@agent-optima/schemas": "*",
    "@fastify/helmet": "^12.0.1",
    "@fastify/rate-limit": "^10.2.2",
    "@fastify/request-context": "^6.0.1",
    "dotenv": "^17.4.0",
    "fastify": "^5.3.2",
    "fastify-plugin": "^5.0.1",
    "jose": "^5.10.0",
    "pino-pretty": "^13.0.0",
    "postgres": "^3.4.5",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3"
  }
}
